'use strict'

const expect = require('chai').expect

describe('SMA indicator behaviour', () => {

  describe('"calculation"', () => {

    it('add me', () => {
      expect('true').to.be.a('string');
    })

  })

})